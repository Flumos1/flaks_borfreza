{
  "name": "flaks-borfreza",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "dev": "vite",
    "prebuild": "npm run seo",
    "build": "vite build",
    "preview": "vite preview",
    "feed": "node scripts/generate-feed.js",
    "landings": "node scripts/generate-landings.js",
    "products": "node scripts/generate-products.js",
    "sitemap": "node scripts/generate-sitemap.js",
    "check:seo": "node scripts/check-seo.js",
    "seo": "npm run landings && npm run products && npm run feed && npm run sitemap && npm run check:seo"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "sharp": "^0.35.4",
    "vite": "^6.0.0"
  }
}
